import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'RELATÓRIOS POR KIT',
    description: '...',
}

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
