import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'AJUSTE DE CAIXAS',
    description: '...',
}

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
