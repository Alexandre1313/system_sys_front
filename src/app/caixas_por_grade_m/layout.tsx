import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'CAIXAS POR GRADE MANUAL',
    description: '...',
}

export default function Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
